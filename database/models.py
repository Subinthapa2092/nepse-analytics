"""
Table schema definitions go here once Phase 1 (full pipeline) starts.

Planned tables:
- companies (symbol, name, sector, listed_shares, ...)
- daily_prices (symbol, date, open, high, low, close, volume)
- floorsheet (transaction-level trade records)
- brokers (broker_id, name)
"""
